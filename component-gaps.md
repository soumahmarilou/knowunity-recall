# Component gaps

A running list of things built inline (from tokens, not a cataloged Storybook component) during a screen build, and which screen each was for. Read before building a new screen — if the same gap shows up a second time, build it as a real component with a story instead of inlining it again.

- ~~**The mic-permission primer card (the "Knowie needs your mic…" confirm card with Allow/Not now) was inlined identically on 4 separate screens**~~ **Resolved** — Guided Reflection's Launched, Concept Questions' Launched, Free Recall Challenge's Launched, and its Aspect-to-revise sub-flow all had byte-identical copies of this card (markup and CSS), and it was never logged here at all until a spec-conformance review caught it — a direct miss of this file's own "flag it, promote on repeat" rule, since nobody had the chance to notice it recurring. Built as a real component per direct instruction (`src/components/MicPermissionPrimer/`, + story), all 4 inline copies replaced. Its `max-width: 280px` is still a raw, un-tokened value (no spacing step lands near 280px) — flagged, not invented, carried over unchanged from the inline version.

- **`TranscriptView` is a wholly new component, not ported from Figma or SPEC.md** — a live "song lyrics"-style scrolling transcript (fade at top/bottom via a `mask-image` gradient, auto-scrolls as text grows), per direct instruction. Needed on every mode's own Recording screen at once (Guided Reflection, Concept Questions, Free Recall Challenge's main loop and its Aspect-to-revise sub-flow), so built as a real component from the start (`src/components/TranscriptView/`, + story) rather than inlined 4 times. `aria-hidden`, not `aria-live` — it updates as often as every ~180ms, which would spam a screen reader if announced. **Updated**: the text it renders now comes from `useSpeechTranscript` (`src/lib/speechTranscript.ts`) — real words via the browser's Web Speech API, a disclosed exception to CLAUDE.md's "no speech-to-text" rule (see that file). `useMockTranscript` (`src/lib/mockTranscript.ts`, scripted filler) is now the fallback for browsers without Web Speech API support, not the primary path.

- **Avatar didn't exist at all** (already flagged in SPEC.md before this build). Needed for **Home chat – Default**'s nav bar. Built as a real component (`src/components/Avatar/`, + story) rather than inlined, per SPEC.md's own build note. Two disclosed substitutions: (1) Figma's instance is `Type=Image`, but no real avatar-image asset or system exists anywhere in this project (and Figma was disconnected, so its actual image fill couldn't even be exported) — renders the `Initials` fallback ("H") instead, a real avatar pattern, not an invented one. (2) The "Large" size's exact pixel value was never confirmed — used `spacing-800` (32px, matching `icon-400`'s value) as a reasonable estimate, not a verified Figma binding.
- **ActivityCard had no way to be tapped.** No `onClick`, rendered a plain `<div>` — despite its own doc comment describing it as a "quick-action entry point." Needed for **Home chat – Default** (the "Recall exercice" card must navigate to `/recall-exercice`). Added an optional `onClick` prop, rendering a real `<button>` when provided — same pattern as `SuperlistItem`'s `showTrailingChevron`. Added the matching button-reset CSS (`border: none`, `cursor: pointer`, `font: inherit`) to `ActivityCard.module.css`, same precedent as `SuperlistItem.module.css`.
- **Seven icons SPEC.md/Figma call for don't exist: `trending-up-01`, `timer-01`, `gauge-01`, `raised-hand-01`, `myai-chat`, `search-md`, `trophy-02`.** (`target-04` was already there.) Needed for **Concept Questions – Intro** (trending-up-01), **Free Recall Challenge – Intro** (trending-up-01/timer-01/gauge-01), and **Home chat – Default** (raised-hand-01, myai-chat, search-md, trophy-02). Not in `Icons.tsx`'s original 24 exports. Built directly as real, cataloged icons (`Icons.tsx` + `Icons.stories.tsx`) rather than inlined per-screen, since icons only ever live in that one shared file anyway — but they're hand-built geometric approximations, not ported from Figma's real vector paths, because the Desktop Bridge plugin was disconnected for every one of these builds (confirmed live, five separate times this session). Anatomy came from each Figma frame's layer *names* where available (stopwatch has a face/hand/pivot/crown/stem; gauge has an arc/needle; trending-up has a line + arrowhead); the four nav/activity icons had no such detail available, just the bare instance name, so those four are closer guesses at a recognizable pictogram (raised hand, chat bubble, magnifying glass, trophy) than the first three. Replace all seven with real exports once Figma's reachable again; see the two `KNOWN GAP` comments in `Icons.tsx`.
- ~~**TextBlock's `variant="L"` doesn't produce "Headline L".**~~ **Resolved** — this entry itself already documented the gap recurring twice (Mode selection, Guided Reflection – Intro) before a spec-conformance review found it a 3rd and 4th time (Concept Questions – Intro, Free Recall Challenge – Intro), all four bypassing `TextBlock` with a hand-styled `<h1>` at `--typography-scale-headline-l-*` directly. A prior version of this entry deferred fixing it to "a third time," which directly contradicted this file's own header rule (fix it on the *second* occurrence); corrected per direct instruction. `TextBlock.module.css`'s `.variantL .title` now points at `typography-scale-headline-l` (33px) instead of `typography-scale-headline-xl` (44px), and all four screens now render a real `<TextBlock variant="L" showCaption={false}>` instead of their own inline heading. **Disclosed regression, not fixed here:** `TextBlock`'s `title` always renders as a `<p>`, never a heading tag — these four screens lose their `<h1>` in the swap. Worth a follow-up on `TextBlock` itself if that matters for this prototype's accessibility bar.

**Update:** a 5th occurrence — **Home chat – Default**'s own `<h1 className={styles.greeting}>` — was never actually on the list above despite its own code comment claiming parity ("Same headline-l situation as every other hero heading built so far — see component-gaps.md"), caught by a later eval pass. Now also converted to `<TextBlock variant="L" showCaption={false} title="Evening study session, Maria?" />`; the original two-line break (a manual `<br/>` after "session,") is gone since `title` only takes a plain string — the line now wraps naturally instead, same disclosed `<p>`-not-`<h1>` regression as the other four.
- **`dots-vertical` icon didn't exist.** Needed for the Guided Reflection session flow's AppBar (the right-side overflow-menu icon, confirmed present in Figma for Launched/Recording/Summary/Summary-Continue). Built directly in `Icons.tsx` — a plain 3-dot vertical glyph, unambiguous enough to build with high confidence despite Figma being disconnected (unlike the placeholder icons logged above, which needed real guesswork).
- **MascotBubble had the same hardcoded-expression problem as MascotSlot before it.** Hardcoded to a bespoke `public/images/mascot-approving.png` asset, no `expression` prop. The Guided Reflection session flow's Processing screen needs it showing "thinking", not "approving". This is the same underlying issue already fixed once for `MascotSlot` (see below) — fixed the same way immediately rather than waiting for a literal second occurrence of *this* component's gap, since it's clearly the same architectural pattern. Added a real `expression` prop (default `"approving"`, keeping the original bespoke asset for that one value); any other expression falls back to the shared `public/images/expressions/*.png` family, same convention as `MascotSlot` — those are a different crop/composition than the bespoke approving art, a disclosed fidelity gap for every expression except the default. Story added (`Thinking`).
- ~~**MascotSlot showing an expression other than "standby".**~~ **Resolved** — showed up a second time (**Mode selection**, needing `excited` same as Guided Reflection – Intro before it), so per this file's own rule it got built properly instead of inlined again: `MascotSlot` (`src/components/MascotSlot/MascotSlot.tsx`) now takes a real `expression` prop (default `"standby"`, same 15 values as `Expressions`), with a story (`MascotSlot.stories.tsx`, `Excited` story added). Guided Reflection – Intro's inline version was removed and replaced with the real component. One side effect worth knowing: the default asset changed from the old one-off `public/images/mascot-standby.png` to `public/images/expressions/standby.png` — same artwork (verified by eye), just consolidated onto the one asset family instead of two near-duplicate files.
- ~~**No "3-step path" illustration exists** (done/current/locked circular nodes joined by a dotted connector). Needed for **Study plan – home**'s "create a plan" graphic.~~ **Superseded** — that empty "create a plan" state was replaced outright by the active-plan build below (`reference/study-plan-overview.png`); this route no longer renders `reference/study-plan-empty-state.png`'s layout at all, so the entry is historical only. The step-node pattern itself carried over, see below.
- **Study plan – home (active-plan build)**: several pieces have no matching Storybook component, all built inline from tokens in `src/app/study-plan/page.tsx`/`page.module.css`: the hero's bordered exam-badge square, the "Maths Exam" title + chevron, the stats row, the Plan/Materials tab pair, and the "Algebraic Fractions 1" header pill. ~~The lesson-step circular nodes reuse the exact done/current/locked badge pattern from the (now-superseded) empty-state build above, just re-themed accent-1 (green) instead of accent-brand (purple).~~ **Superseded twice over** — first the accent-1 fill was swapped for `feedback-success`/`feedback-info` (a separate, earlier fix, since `accent/1-4` has no fixed status meaning per design-system_1.md's "Never do this" list), then the hand-rolled circle itself was replaced entirely — see the new entry below. Two new hand-built placeholder icons (`CalendarIcon`, `ExamIcon` — a checkmark, redrawn once already after a first draft read ambiguously as a "<>" bracket at this size) since no calendar or exam glyph exists in this project's icon set and Figma's Desktop Bridge was disconnected for this build. Flag any of these for promotion to a real component if the same shape is needed by a second screen.
- **`Screen`'s `min-height: 100dvh` doesn't clamp content to the viewport** — fine for every earlier screen since their content happened to fit, but **Study plan – home**'s content (hero + tabs + a 4-step lesson list) is the first to exceed one viewport, so `.middleContent`'s `flex: 1` had nothing to shrink against and the whole page grew past 844px, pushing the bottom nav bar off-screen (caught via a Playwright functional test, not visually — the screenshot alone didn't show it was cut off). Fixed page-locally: `Screen`'s `className` prop now takes a page override (`.screen { height: 100dvh; }` in that page's own module) plus `.middleContent { min-height: 0; overflow-y: auto; }`, so the middle content scrolls internally while the top bar and bottom nav stay pinned in view. Not promoted into `Screen` itself since no other screen needs it yet — flag if a second screen hits the same overflow.
- **The bottom nav bar's 3rd tab (`target-04`, the multi-tone "colored icon family" version) was never actually a valid choice for `NavigationButton`**, which forces every icon's fill to `currentColor` for its Active/Inactive states — correct for its other three icons (`myai-chat`/`search-md`/`trophy-02`, all single-tone) but not for a two-tone illustrated icon, which collapsed into an unrecognizable flat blob once actually set to Active (never caught before now because that tab had no route and was never wired active). Fixed by building `target-01`, a proper single-tone bullseye matching the other three nav icons' own construction, and swapping all three nav-bar usages (`src/app/page.tsx`, `src/app/recall-exercice/page.tsx`, `src/app/study-plan/page.tsx`) to it. `target-04` itself is untouched and still available for any real "colored icon family" use.
- **No redo glyph exists anywhere in this project's icon set or Figma's own library.** Needed for every mode's Recording/Processing "Redo" `ButtonIcon` (was reusing `trash-02`/`trash-01` as a stand-in). Per direct instruction, built as `Redo01` in `Icons.tsx` — but sourced from the `@mdi/react` + `@mdi/js` Material Design Icons npm package instead of a hand-drawn SVG, the one real exception to every other icon in that file being hand-built to match Figma exactly. `size="100%"` (not a fixed px value) keeps it compatible with `IconSlot`'s own sizing system. Same fix also repositioned every Recording/Processing screen's controls row (`Redo` now sits immediately left of `Send`, with an invisible mirrored spacer column keeping `Send`/the mic button pinned to the row's true center regardless of `Redo`'s width) — confirmed pixel-identical between Recording and Processing via a bounding-box check, and confirmed the invisible spacer doesn't leak into the accessibility tree.
- **Study plan's lesson-step icons were a hand-rolled circle, not a cataloged component** (see the now-superseded entry above) — replaced per direct instruction with real `ButtonIcon` instances. Checked live against Figma (Desktop Bridge connected this session) before building anything: white (current) and grey (locked) turned out to need nothing new at all — `variant="Primary" state="Default"` and `variant="Primary" state="Disabled"` already existed and already matched the old hand-rolled tokens exactly once actually compared. Only two things were genuinely new:
  - **`ButtonIcon`'s `Success` variant** (green, for the done step) — added to the real Figma component set (`buttonIcon`, node `9003:8235`) as a fourth `variant` option, `feedback-success-bold` fill / `feedback-success-on-bold` icon color, built out to the full `S`/`M`/`L` × `Default`/`Pressed`/`Disabled`/`Loading` grid per direct instruction (matching `Primary`/`Secondary`/`Tertiary`'s own completeness, not just the one cell Study plan's done step actually uses). `Disabled` collapses to the same universal `background/surface` treatment every other variant's `Disabled` already uses. Went through two real corrections before landing here, both logged plainly rather than smoothed over: it was first built with no border/shadow at all (read as a flat colored dot once seen rendered, not a real button — fixed to match `Primary`'s border + inner shadow), and its `Pressed` state briefly kept that shadow with an overlay tint layered on top (a mechanism re-checked directly against Figma's real `Primary`/`Secondary` `Pressed` components at the time and genuinely accurate to what was there then) before a final correction below changed what `Pressed` means for the whole component, including Figma itself.
  - **`ButtonIcon`'s `Pressed` state, for real this time, across `Primary`/`Secondary`/`Success`** — per direct instruction, matches `button`'s own established convention: the inner shadow flattens to `none` on press, paired with the icon settling down by the same amount the shadow used to offset it (`.iconWrap`, a `translateY` on the icon inside the button, 2px at S/M and 4px at L). This is the third and final version of this rule in one session: the first (before ever checking Figma) assumed shadow-removal by analogy to `button` and was correctly challenged for that; re-checking Figma's own `Primary`/`Secondary` `Pressed` components at that point showed the shadow staying with an overlay tint added instead, which got built and shipped; the actual intent, confirmed directly afterward, was for `buttonIcon` to match `button`'s convention regardless of what its own static Figma `Pressed` variants happened to show — so this final version updates **both** Figma (all 9 `Primary`/`Secondary`/`Success` × `S`/`M`/`L` `Pressed` components, shadow removed and icon shifted down, via `figma_execute`) and code together, so the two don't quietly diverge again.
  - **`Progress Ring`** (`src/components/ProgressRing/`, + story) — a wholly new component, no existing match anywhere; the only progress component that existed (`progressIndicator`) is explicitly documented as "a horizontal bar, not a ring." Also built as a new Figma component set (5 variants, `progress` stepped in quarters like `progressIndicator`'s own convention, native Figma `arcData` for the arcs) before the code. Wraps the current step's `ButtonIcon`, reading `?progress=` on `/study-plan` (parsed, clamped, snapped to the nearest of 0/25/50/75/100, default `0` when absent) — this prototype has no real session-progress persistence to read from, so the URL param is the disclosed stand-in, same category as this app's other query-param-driven demo state (`?subject=`, `?entry=`). Pulses gently (`scale(1)` ↔ `scale(1.04)`, 2.4s loop) to invite a tap; unlike `Button`/`ButtonIcon`'s own `Loading` spinner (which slows under `prefers-reduced-motion` but keeps moving, since it reports real state), the pulse stops outright under reduced motion since it's decorative, not informational. Ring stroke width (4.5px) is a disclosed raw value — no stroke-width token in `tokens.json` lands anywhere near it (only `stroke-border`/`stroke-heavy-border`, 1px/2px, exist).
  - Side effect: `ButtonIcon`'s real `L` size is 56px, not the 64px the old hand-rolled `.node` used — using the real component's real size (rather than inventing an in-between value) is a size correction, not a regression; the new ring's own 64px outer diameter keeps the row's visual footprint the same as before.
  - Tappable area also narrowed as part of this change, per direct instruction: `.step` is no longer a `<button>` wrapping icon+label — the `ButtonIcon` alone is the control now, and the label next to it is plain, inert text. Each `ButtonIcon`'s `aria-label` carries the full standalone context as a result (e.g. `"Algebraic Fractions, completed"`, `"Recall exercice, continue, 50% complete"`), since there's no adjacent button text left to fall back on for the accessible name.
- **`ButtonIcon` never actually applied its own `color` to the icon it renders.** Found while checking why the "Recall exercice" step's icon rendered grey instead of `interactive/onPrimary`: this project's `Icons.tsx` bakes a fixed token color into each icon's own SVG `fill` attribute (e.g. `Microphone01`'s path hardcodes `fill="var(--color-text-secondary)"`) — correct for an icon's normal, single-color usage elsewhere, but `ButtonIcon.module.css` never added the `fill: currentColor` override every other icon-tinting component in this app already has (`AppBarButtonIcon`, `NavigationButton`, `MicButton`, `IconBadge`, `Button`), so every icon inside a `ButtonIcon` silently ignored whichever color its `variant` actually declared. Fixed with the same `.button :global(svg *) { fill: currentColor; }` pattern those other components use. Visible effect beyond the one bug report: two icons that happened to be hardcoded to a decorative `accent-1` token (`BlankPage01` on Study plan's "View materials", `StackSparkle01` on the locked steps) now correctly show their real variant/state color instead — `interactive/onSecondary` and `text/disabled` respectively — which is also a `design-system_1.md` "Never do this" violation (`accent/1-4` for anything but decoration) quietly resolved as a side effect, not something newly broken by this fix.
- **`Progress Ring` (the Figma component, not the code one) went missing.** First built in an empty area of the canvas disconnected from `buttonIcon` — undiscoverable there, per direct feedback — and by the time of a later pass its node no longer resolved at all and it had dropped out of the local component search entirely, most likely deleted while it looked like an orphaned duplicate sitting off on its own (this file's own history already names that exact failure mode — see `Mascot bubble`'s stray `position` variant, found and removed the same way for the same reason). The React component (`src/components/ProgressRing/`) was never touched and stayed correct throughout — only its Figma counterpart was affected. Rebuilt identically (same tokens, same `arcData` construction, same 5-step grid), this time placed directly beside `buttonIcon`'s real library frame instead of isolated canvas space, plus a real composed instance (`buttonIcon` Primary/L/Default + `Progress Ring`, positioned the way `/study-plan`'s current step actually pairs them) placed next to both component sets — so the pairing is visible on the canvas itself, not just described in text.
- **`ChatInput` had no `disabled` prop at all** — added one (textarea gets a real HTML `disabled`, plus the trailing mic/send button), wired on Guided Reflection's own Processing screen so the mic and chat input are both genuinely locked while Knowie reacts, not just visually inert via a no-op `onChange` (which still left the field typable). **Flagging, not fixing**: Concept Questions' and Free Recall Challenge's own Processing screens (`session/processing/ProcessingContent.tsx` in each mode) almost certainly have the identical gap — they follow the same `MicButton`-disabled/`ChatInput`-not-disabled pattern — but only Guided Reflection was named for this fix, so the other two are unwired still. Worth a pass to add `disabled` there too before assuming this is handled everywhere.
- **The Study plan exit-progress checkpoints undercounted "mid-exercise."** `entryPoint.ts`'s `ExitCheckpoint` originally gave every mode's own Session/Launched screen (the actual question prompt) the same `0` as its Intro screen — meaning leaving right after engaging with a real question looked identical to never having opened the mode at all, reported as "I don't see any advancement" when leaving mid-exercise. Rebalanced per direct instruction: Intro stays `0`, Session moves to `25`, Recording to `50`, Processing/Reveal/After-recording and everything past that to `75` (`100` stays reserved for genuine completion via `studyPlanReturnUrl`). Same mechanical sed-driven remap across the same ~9 `SessionContent.tsx`/`RecordingContent.tsx`/`ProcessingContent.tsx` files the original checkpoint wiring touched.
- **`ProgressRing`'s track was too faint to register as progress at low fill values.** `border/default` (the token it originally used) is a ~10% white overlay — close to invisible against the app's dark background, so a 25% fill read as a stray green sliver with nothing visible to show it was "part of a ring" rather than actual progress. Swapped to `border/strong` (~25% white, its own token description literally says "use for dividers that must read at a glance") — same fix applied to both the Figma `Progress Ring` component (all 5 `progress=` variants) and the code, together, so they don't diverge the way `ButtonIcon`'s `Pressed` state did earlier this session.
- **A small "icon + exercise name" eyebrow above each mode Intro's title has no matching Storybook component, and was inlined identically 3 times in one pass** — Guided Reflection, Concept Questions, and Free Recall Challenge's own Intro screens (`.titleGroup`/`.exerciseLabel`/`.exerciseLabelText` in each screen's own `page.module.css`, byte-identical CSS in all three). Per direct instruction, each Intro's explanatory benefit list was folded into the title itself, with this small identity row placed above it (reusing each mode's own canonical icon from Mode selection — `Microphone01`/`Lightbulb01`/`GraduationHat01` — rather than the unrelated icons some of these screens used before). Landed as 3 duplicated inline copies rather than one shared component up front, since all three needed it in the same pass with no earlier single-screen occurrence to build from — flagged here per this file's own "flag it, promote on repeat" rule rather than silently left duplicated. Worth promoting to a real component (something like an `ExerciseLabel`, `IconSlot` size `150` + a caption-m text pair) if a fourth screen ever needs the same identity marker.
- **`MascotBubble` gained a new `overline` prop, not a Figma property** — a small, thin line ("Question 1 of 3") above the chip/body, per direct instruction, replacing the "1/3"-style counter that used to sit in the AppBar next to Concept Questions' progress bar on all four of that mode's screens (Launched, Recording, Processing, Reveal). Same "extend the existing component" precedent `chipColor` already set on this component, not a one-off hand-rolled element per screen — real prop, real Storybook story (`WithOverline`), documented in the component's own header comment.
- **Concept Questions' progress bar used to pre-fill for whichever term was currently showing** (term 2 already read 50% before the student had done anything with it) — per direct instruction, it now fills only once a term has actually concluded (any outcome — first-try, hinted, or a forced reveal — counts as "concluded," not just a clean pass; see `getProgressFromOutcomes` in `terms.ts` for the full reasoning, including why success-only gating was considered and rejected). Disclosed interpretation call, not written down anywhere before this: the original request said the bar should fill "each time a question is passed (succeeded)," which could also have meant gating strictly on non-revealed outcomes — flag if that stricter reading was actually intended, since it's a one-line change (`outcomes.filter(o => o !== "revealed").length` in place of `outcomes.length`).
- **`IconBadge` gained a new `size="L"` variant, code-only, not mirrored in Figma.** Per direct instruction: the mascot is now gone from all three mode Intros, replaced by this component at hero scale (120×120, icon 64×64 — same footprint as `MascotSlot`'s own `2XL` step) showing each mode's own identity icon instead. A live Figma edit for the matching `iconBadge` component-set variant was started this session and explicitly declined — the code-side component, its Storybook story (`Large`), and all three Intro screens are done and real, but `iconBadge` in Figma itself still only has the original 32×32 size. Flag before assuming the two are in sync; do the Figma-side addition as its own explicitly-requested step.
- **`/recall-exercice`'s own greeting still said "Harry" and wasn't wrapped correctly.** Missed when Home chat's identical hero heading was converted from a hand-rolled `<h1>` to `TextBlock` (see that earlier entry above) — this screen never got the same pass, so it kept two bugs Home already had fixed: the name ("Harry", while every other screen already reads "Maria" — same root cause as `Avatar`'s `initials="H"`, also fixed here, on this screen, Home, and Study plan, all three found still saying "H") and the line wrap (a bare `<br/>` after "Evening study session," with nothing stopping that phrase from itself wrapping onto 2 lines first on a real device's system font, producing 3 lines total instead of the intended 2 — Home's own fix already establishes the real cause: no font file is embedded in this project, see layout.tsx's comment, so wrap points move per device/font). Fixed the same way Home was: converted to `TextBlock`, with the same `"Evening study session, Maria?"` non-breaking-space string (locks the first three words together as one unit, leaves the space before "Maria?" as the one real break point) — confirmed byte-for-byte identical to Home's own fixed string, not just visually similar.
